{
  "//": "Mirrors how Vercel compiles the api/ folder: it uses the ROOT tsconfig's compilerOptions with CommonJS defaults, NOT tsconfig.app.json. Kept as a separate project so `npm run typecheck` catches api-only breakage locally instead of at deploy time.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "module": "CommonJS",
    "moduleResolution": "Node10",
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "types": ["node"]
  },
  "include": ["api"],
  "references": []
}
